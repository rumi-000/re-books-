　 <% if book.errors.any? %>
    <div>
      <h2><%= @book.errors.count %>errors occurred</h2>
          <%#= pluralize(book.errors.count) %>
      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <%= form_with model:book do |f| %>
  <%#= form_with model:book local:true do |f| %>
    <div>
    <%#= f.label :title %>
    <%= f.text_field :title %>
    </div>

    <div>
    <%#= f.label :body %>
    <%= f.text_area :body %>
    </div>
    <div>
    <%= f.submit 'Create Book'%>
    </div>
   <% end %>


<!--定義
<!-index:book=@book(=Book.new)>

 